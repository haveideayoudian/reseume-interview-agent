# 开发方法论：如何保证 Agent 不偏离主线

## 核心原则

### 1. 目标导向
每次开发前，明确：
- **当前任务**：做什么功能
- **验收标准**：什么时候算完成
- **后续影响**：对整体项目的影响

### 2. 最小闭环
每次只做一件事，完成一个完整闭环：
- 输入 → 处理 → 输出 → 测试
- 不做"看起来有用"但偏离主线的功能

### 3. 代码复用优先
能用现有工具，不重复造轮子：
- 不要为每个 Agent 都写 LLM 配置
- 不要为每个 Agent 都写工具注册
- 用 `base.py` 做基础配置，其他 Agent 继承

---

## 开发流程

### Step 1：需求拆解
把大目标拆成小任务：
```
大目标：简历优化 Agent
  ↓
小任务1：能读取 JD 和简历文件
  ↓
小任务2：分析匹配度
  ↓
小任务3：给出优化建议
  ↓
小任务4：润色简历
```

### Step 2：设计输入输出
明确：
- **输入**：用户给什么
- **输出**：Agent 返回什么
- **中间数据**：需要存储什么

### Step 3：编写代码
- 先写核心逻辑
- 再写测试
- 最后优化

### Step 4：测试验证
- 单元测试：测试每个 Agent
- 集成测试：测试完整流程
- 用户测试：让用户试用

### Step 5：记录决策
每次做决定时，记录：
- 为什么这么设计
- 有什么替代方案
- 优缺点是什么

---

## 目录结构设计

### 原则
1. **按功能划分**：每个目录代表一个功能模块
2. **依赖关系清晰**：外层依赖内层，内层不依赖外层
3. **易于扩展**：新增功能不影响现有代码

### 推荐结构
```
resemu_interview/
├── .env                    # 环境变量（敏感信息）
├── requirements.txt        # 依赖列表
├── README.md              # 项目说明
├── AGENTS.md              # 开发方法论（本文档）
├── src/
│   ├── __init__.py
│   ├── agents/            # Agent 定义
│   │   ├── __init__.py
│   │   ├── base.py        # 基础 Agent（LLM 配置）
│   │   ├── jd_analyzer.py # JD 分析 Agent
│   │   ├── resume_analyzer.py # 简历分析 Agent
│   │   ├── matcher.py     # 匹配度评估 Agent
│   │   └── optimizer.py   # 优化润色 Agent
│   ├── tools/             # 工具函数（通用）
│   │   ├── __init__.py
│   │   └── file_reader.py # 文件读取工具
│   ├── core/              # 核心逻辑
│   │   ├── __init__.py
│   │   └── prompts.py     # Prompt 模板
│   └── utils/             # 通用工具
│       ├── __init__.py
│       └── helpers.py     # 辅助函数
├── api/                   # API 接口（FastAPI）
│   ├── __init__.py
│   └── main.py
├── web/                   # Streamlit 前端
│   ├── __init__.py
│   └── app.py
├── tests/                 # 测试代码
│   ├── __init__.py
│   └── test_agents.py
└── data/                  # 示例数据
    ├── sample_jd.txt
    └── sample_resume.txt
```

---

## 文件命名规范

### Agent 文件
- `jd_analyzer.py` - JD 分析 Agent
- `resume_analyzer.py` - 简历分析 Agent
- `matcher.py` - 匹配度评估 Agent
- `optimizer.py` - 优化润色 Agent

### 工具文件
- `file_reader.py` - 文件读取工具
- `text_cleaner.py` - 文本清洗工具

### 配置文件
- `base.py` - 基础配置（LLM、工具等）
- `prompts.py` - Prompt 模板

---

## 避免常见错误

### 错误1：过度设计
- **表现**：一开始就想好所有功能
- **后果**：代码复杂，难以维护
- **解决**：先做 MVP，后续迭代

### 错误2：重复造轮子
- **表现**：每个 Agent 都写一遍 LLM 配置
- **后果**：代码冗余，难以统一管理
- **解决**：用 `base.py` 做基础配置

### 错误3：偏离主线
- **表现**：做了很多"有用"但不是核心功能的东西
- **后果**：浪费时间，增加复杂度
- **解决**：每次开发前问自己：这功能对核心目标有用吗？

### 错误4：缺乏测试
- **表现**：写完代码直接用
- **后果**：Bug 难以发现，修复困难
- **解决**：先写测试，再写代码

---

## 扩展性设计

### 为未来功能预留接口
- **工具层**：新工具统一放在 `src/tools/`
- **核心层**：新 Prompt 模板放在 `src/core/prompts.py`
- **Agent 层**：新 Agent 继承 `base.py` 的配置
- **API 层**：新接口放在 `src/api/`

### 保持接口稳定
- Agent 的输入输出格式尽量不变
- 工具的接口保持稳定
- 即使内部实现变化，对外接口不变

---

## 每次开发的检查清单

### 开发前
- [ ] 明确当前任务
- [ ] 拆解成小步骤
- [ ] 设计输入输出
- [ ] 预估开发时间

### 开发中
- [ ] 只做当前任务
- [ ] 代码复用现有工具
- [ ] 编写测试用例
- [ ] 记录关键决策

### 开发后
- [ ] 运行测试
- [ ] 验证功能
- [ ] 更新文档
- [ ] 提交代码

---

## 总结

**记住：**
1. 目标导向，不做偏离主线的事
2. 最小闭环，一个任务一个任务完成
3. 代码复用，不要重复造轮子
4. 文件清晰，按功能划分目录
5. 测试先行，确保质量

**原则：**
- MVP 先行，迭代优化
- 接口稳定，易于扩展
- 文档完整，便于维护
- 代码简洁，易于理解
